# Go 学习笔记

## 目录结构规划

- 基础语法
- 并发编程（goroutine / channel）
- 标准库
- 实战练习

## 进度

📌 **已启动（2026-08-04）**：方向为「配合数据库内核/存储系统学习」，第一组已规划但内容未交付。

### 已规划内容（待学习）

**第 1 组：内存模型（C→Go 心智映射）**
1. **C→Go 心智映射**：值语义/引用语义、栈 vs 堆、指针的异同
2. **slice 底层三件套**：`data` 指针 + `len` + `cap`（类比：切片是数组的"视图 + 长度 + 容量"）
3. **逃逸分析**：编译器决定变量去栈还是去堆（内存分配优化）
4. **结构体内存布局**：与 C 对齐规则类比
5. **零值 / 复合字面量**：`var x int` → 0，`nil` 语义
6. **GC 与存储性能的关系**：垃圾回收对高吞吐存储服务的影响

> ⏳ 后续：确认继续后按 5 概念/组 → 练习 → 批改流程补齐。
